<Page
    x:Class="PhoneMic.App.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer Padding="24" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16" MaxWidth="720" HorizontalAlignment="Left">
            <TextBlock Text="Панель" FontSize="28" FontWeight="SemiBold"/>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="{x:Bind Vm.SessionStatus, Mode=OneWay}" FontSize="18" FontWeight="SemiBold"/>
                    <TextBlock Text="{x:Bind Vm.DriverInfo, Mode=OneWay}" Foreground="{ThemeResource SystemFillColorCautionBrush}" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" Spacing="24">
                        <TextBlock Text="{x:Bind Vm.PhoneDevice, Mode=OneWay}"/>
                        <TextBlock Text="{x:Bind Vm.TransportKind, Mode=OneWay}"/>
                        <TextBlock Text="{x:Bind Vm.CodecInfo, Mode=OneWay}"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <Grid ColumnSpacing="16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Spacing="4">
                            <TextBlock Text="Уровень микрофона"/>
                            <ProgressBar Value="{x:Bind Vm.Level, Mode=OneWay}" Maximum="1" Height="10"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Spacing="4">
                            <TextBlock Text="Статус"/>
                            <Ellipse Width="18" Height="18" Fill="LimeGreen"
                                     Visibility="{x:Bind Vm.StreamActive, Mode=OneWay}"/>
                            <Ellipse Width="18" Height="18" Fill="Gray"
                                     Visibility="{x:Bind Vm.StreamActive, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Orientation="Horizontal" Spacing="24">
                        <TextBlock Text="{x:Bind Vm.RttLabel, Mode=OneWay}"/>
                        <TextBlock Text="{x:Bind Vm.LossLabel, Mode=OneWay}"/>
                        <TextBlock Text="{x:Bind Vm.BufferLabel, Mode=OneWay}"/>
                        <TextBlock Text="{x:Bind Vm.UnderrunsLabel, Mode=OneWay}"/>
                    </StackPanel>

                    <StackPanel>
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="70"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Громкость" VerticalAlignment="Center"/>
                            <Slider Grid.Column="1" Minimum="0" Maximum="1.5" StepFrequency="0.05"
                                    Value="{x:Bind Vm.Volume, Mode=TwoWay}"
                                    ValueChanged="Volume_ValueChanged"/>
                            <TextBlock Grid.Column="2" Text="{x:Bind Vm.VolumeLabel, Mode=OneWay}" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="12">
                        <Button Click="Mute_Click">
                            <TextBlock Text="{x:Bind Vm.Mute, Mode=OneWay, Converter={StaticResource MuteLabelConverter}}"/>
                        </Button>
                        <ToggleButton IsChecked="{x:Bind Vm.Active, Mode=TwoWay}" Click="Active_Click">
                            <TextBlock Text="{x:Bind Vm.Active, Mode=OneWay, Converter={StaticResource ActiveLabelConverter}}"/>
                        </ToggleButton>
                        <Button Content="Стоп" Click="Stop_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
